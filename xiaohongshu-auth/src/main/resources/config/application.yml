server:
    port: 8080
spring:
    profiles:
        active: dev #默认启动开发环境
mybatis:
    mapper-locations: classpath:mapper/**/*.xml # MyBatis XML 映射文件位置
logging:
    level:
      com.zry.xiaohongshu.auth.domain.mapper: DEBUG # 设置日志级别为 DEBUG
spring:
  application:
    name: xiaohongshu-comment
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: http://127.0.0.1:8848/ # 指定 Nacos 配置中心的服务器地址
        prefix: ${spring.application.name} # 配置 Data Id 前缀，这里使用应用名称作为前缀
        group: DEFAULT_GROUP # 所属组
        namespace: xiaohongshu # 命名空间
        file-extension: yaml # 配置文件格式
        refresh-enabled: true # 是否开启动态刷新
      discovery:
        enabled: true
        namespace: xiaohongshu # Nacos 服务发现的命名空间
        group: DEFAULT_GROUP # Nacos 服务发现的组
        server-addr: http://127.0.0.1:8848/